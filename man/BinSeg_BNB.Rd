% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binseg_bnb.R
\name{BinSeg_BNB}
\alias{BinSeg_BNB}
\title{Binary Segmentation for Bivariate Negative Binomial Data}
\usage{
BinSeg_BNB(
  CGH,
  prior = "constant",
  th_cp = 0.5,
  k_val = 5,
  v1 = 2,
  v2 = 1,
  v3 = 1,
  save_output = FALSE,
  file_name = "bivnegbincp_output.xlsx"
)
}
\arguments{
\item{CGH}{A two-column matrix or data frame of non-negative integer counts.}

\item{prior}{Character string. Either \code{"constant"} or \code{"dirichlet"}.}

\item{th_cp}{Numeric threshold for retaining detected change points.}

\item{k_val}{Positive numeric fixed value of the dispersion parameter.}

\item{v1}{Positive numeric hyperparameter for the Dirichlet prior.}

\item{v2}{Positive numeric hyperparameter for the Dirichlet prior.}

\item{v3}{Positive numeric hyperparameter for the Dirichlet prior.}

\item{save_output}{Logical. If \code{TRUE}, writes the returned table to an Excel file.}

\item{file_name}{Output Excel file name when \code{save_output = TRUE}.}
}
\value{
A data frame with columns \verb{Posterior Probability} and \verb{CP index}.
Returns \code{NA} when no change point exceeds the threshold.
}
\description{
Applies the bivariate Negative Binomial single change point detector
recursively to identify multiple change points through binary segmentation.
The prior is selected through the \code{prior} argument and the matching internal
single-change routine is used at each step.
}
\examples{
set.seed(123)
x1 <- c(rpois(20, 8), rpois(15, 20), rpois(15, 10))
x2 <- c(rpois(20, 2), rpois(15, 8), rpois(15, 3))
dat <- cbind(x1, x2)
BinSeg_BNB(dat, prior = "constant", th_cp = 0.15, k_val = 1)

}
