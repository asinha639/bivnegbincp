% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cp_3d.R
\name{plot_cp_3d}
\alias{plot_cp_3d}
\title{Interactive 3D Plot of Bivariate Time Series and Change Points}
\usage{
plot_cp_3d(
  x1,
  x2,
  change_points,
  var1_name = "Variable 1",
  var2_name = "Variable 2",
  time_name = "Time",
  theme = "dark",
  point_size = 5,
  cp_size = 7
)
}
\arguments{
\item{x1}{A numeric vector for the first variable.}

\item{x2}{A numeric vector for the second variable.}

\item{change_points}{A numeric vector of detected change point indices.}

\item{var1_name}{Character string for the first variable axis label.
Default is \code{"Variable 1"}.}

\item{var2_name}{Character string for the second variable axis label.
Default is \code{"Variable 2"}.}

\item{time_name}{Character string for the time axis label.
Default is \code{"Time"}.}

\item{theme}{Character string specifying the visual theme. Either \code{"dark"}
or \code{"light"}. Default is \code{"dark"}.}

\item{point_size}{Numeric marker size for the main data points.
Default is \code{5}.}

\item{cp_size}{Numeric marker size for the highlighted change points.
Default is \code{7}.}
}
\value{
A plotly interactive 3D plot object.
}
\description{
Generates an interactive 3D plot using plotly to visualize a bivariate time
series, with detected change points highlighted in a polished 3D display.
The first two axes correspond to the two observed variables, while the third
axis represents time.
}
\examples{
\dontrun{
set.seed(123)
x1 <- c(rpois(20, 20), rpois(15, 55), rpois(15, 95))
x2 <- c(rpois(20, 10), rpois(15, 45), rpois(15, 18))
cp <- c(20, 35)

plot_cp_3d(
  x1 = x1,
  x2 = x2,
  change_points = cp,
  var1_name = "Cases",
  var2_name = "Deaths",
  time_name = "Day",
  theme = "dark"
)
}

}
