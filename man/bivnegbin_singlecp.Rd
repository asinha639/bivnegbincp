% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bivar_negbin_cp.R
\name{bivnegbin_singlecp}
\alias{bivnegbin_singlecp}
\title{Bivariate Negative Binomial Single Change Point Detection}
\usage{
bivnegbin_singlecp(
  x1,
  x2,
  prior = "constant",
  kappa_const = 1,
  v1 = 2,
  v2 = 1,
  v3 = 1
)
}
\arguments{
\item{x1}{Integer-valued numeric vector for the first count series.}

\item{x2}{Integer-valued numeric vector for the second count series.}

\item{prior}{Prior specification. Must be either \code{"constant"} or
\code{"dirichlet"}. Capitalization is ignored.}

\item{kappa_const}{Positive numeric value for the fixed dispersion parameter.}

\item{v1}{Positive numeric hyperparameter for the Dirichlet prior.}

\item{v2}{Positive numeric hyperparameter for the Dirichlet prior.}

\item{v3}{Positive numeric hyperparameter for the Dirichlet prior.}
}
\value{
A one-row data frame with columns \code{post_prob}, \code{cp_index}, and
\code{cp_status}. The value \code{post_prob} is the posterior probability at the
selected change point, \code{cp_index} is the estimated change point location,
and \code{cp_status = 1} indicates that the selected location is not too close
to the boundary.
}
\description{
Detects a single change point in bivariate count data assumed to follow a
bivariate Negative Binomial model. The user chooses the prior through the
\code{prior} argument. This function serves as the core single-change-point
routine used by the binary segmentation procedure for multiple change point
detection.
}
\examples{
set.seed(123)
x1 <- c(rpois(20, 8), rpois(20, 20))
x2 <- c(rpois(20, 2), rpois(20, 8))
bivnegbin_singlecp(x1, x2, prior = "constant", kappa_const = 1)

}
